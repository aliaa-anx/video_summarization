package com.backend_microservices.auth_service.entity;

// this enum is a fixed list of allowed roles, example: role.setRoleName(RoleName.ROLE_USER.name())
public enum RoleName {
    ROLE_USER,
    ROLE_ADMIN
}
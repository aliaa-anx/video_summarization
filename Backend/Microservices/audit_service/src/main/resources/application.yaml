server:
  port: 8085

spring:
  application:
    name: audit-service

  elasticsearch:
    uris: http://localhost:9200

eureka:
  instance:
    prefer-ip-address: true # This tells Eureka to share the IP, not the Laptop name
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka